{
  "name": "create-ced-version-action",
  "version": "1.0.0",
  "description": "Github Action to create an empty version within a CloudEdgeDistribution project.",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -r ./lib && rm -r node_modules"
  },
  "author": "Merthin Technologies",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.2.4",
    "@ced/cli-dev": "^1.0.35-dev"
  },
  "devDependencies": {
    "@types/node": "^13.1.2",
    "typescript": "^3.7.4"
  }
}
